import * as React from "react";
import { render } from "react-dom";

import { VideoPlayback } from "./App";

const rootElement = document.getElementById("root");
render(<VideoPlayback />, rootElement);
